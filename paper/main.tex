\documentclass{article}

\PassOptionsToPackage{numbers,sort&compress}{natbib}
\PassOptionsToPackage{dvipsnames,table}{xcolor}
\usepackage[dblblindworkshop]{neurips_2026}
\hyphenation{GaussMark dgMARK Shibboleth LLaDA}
\workshoptitle{Foundations of Language Model Security}
% The style file uses \@trackname only for camera-ready, so the submission notice
% would otherwise still read "Submitted to 40th Conference ... (NeurIPS 2026)".
\makeatletter
\renewcommand{\@noticestring}{%
  Submitted to the NeurIPS \@neuripsyear\ Workshop on Foundations of Language Model Security.
  Do not distribute.%
}
\makeatother
\usepackage{xcolor}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\usepackage{wrapfig}
\floatstyle{ruled}
\newfloat{algorithm}{tbp}{loa}
\floatname{algorithm}{Algorithm}
\usepackage{enumitem}
\usepackage{amsthm}
\theoremstyle{definition}
\newtheorem{proposition}{Proposition}
\usepackage{microtype}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,decorations.pathreplacing}

% Float spacing: NeurIPS defaults leave ~20pt around every float.
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{4pt}
\setlength{\belowcaptionskip}{0pt}

% Restrained, grayscale-safe accents shared by figures and tables.
\definecolor{oursemph}{RGB}{24,84,140}
\definecolor{finalres}{RGB}{242,239,232}

\usepackage[breaklinks,colorlinks,linkcolor=oursemph,citecolor=oursemph,urlcolor=oursemph]{hyperref}
\usepackage{url}

% Draft placeholder for a cell that has no measurement yet.
\newcommand{\tbd}{\textemdash}
\newcommand{\needcite}{\textcolor{BrickRed}{\textbf{[cite?]}}}

\title{Shibboleth: Probe-and-Replay Model-Response\\Watermarking for Diffusion Language Models}
\author{Anonymous}

\begin{document}
\maketitle

\begin{abstract}
Text watermarks usually hide a secret in token choices and later test those tokens without
rerunning the generator. We ask whether a masked diffusion language model (DLM) can instead help
verify its own output. Hiding selected parts of the context changes the probability of an emitted
token in a reproducible, checkpoint-specific direction. Shibboleth uses that model response as the
watermark carrier.

The method first finds positions where plausible candidates exhibit both response directions.
Only after a position is fixed does the key choose which direction to favour. The generator then
resamples from its current distribution without changing the decoding schedule. To verify a
finished document, Shibboleth reconstructs the same masked contexts and counts how often the
responses agree with the key. Position selection cannot observe the requested direction, so text
produced independently of the key gives an exact binomial count.

At $1\%$ FPR, Shibboleth reaches $0.80$ TPR on LLaDA-8B-Instruct and $0.90$ on
Dream-7B-Instruct. The strongest token-local baseline reaches $0.93$ on LLaDA but $0.57$ or less
on Dream when its LLaDA setting is transferred unchanged. At Shibboleth's main setting, the PPL
ratio is $0.74\times$ on LLaDA and $1.09\times$ on Dream, and changes across the measured task
scores are at most $0.06$. This checkpoint binding has a cost: verification needs the same model
and 144 full-sequence calls per 512-token document, and dispersed edits reduce detection power.
\end{abstract}

\input{sections/introduction}
\input{sections/related_work}
\input{sections/preliminaries}
\input{sections/method}
\input{sections/experiments}
\input{sections/discussion}

\section{Conclusion}
\label{sec:conclusion}

Shibboleth establishes model response as a watermark channel for masked DLMs. Its signal is a
reproducible change in model probability under hidden context, rather than membership in a keyed
token subset. The central lesson is the ordering: select a carrier with a symmetric response score,
then reveal the keyed orientation. This separation yields an exact binomial test even when the text
and model determine how many carriers are available.

The method complements token-local watermarking rather than replacing it. It preserves the
deployed decoding schedule and reaches $0.80$ TPR on LLaDA and $0.90$ on Dream with settings
transferred without retuning. In return, the verifier must replay the same checkpoint 144 times per
512-token document. Token-local detectors remain cheaper and reach higher TPR on LLaDA.

\paragraph{Limitations.}
The false-positive guarantee does not imply detection after editing. Random $10\%$ re-denoising
reduces TPR from $0.35$ to $0.05$ at $R=1$ and from $0.88$ to $0.50$ at $R=16$, although one
contiguous edited span leaves the measured TPR unchanged. Replay also requires the same checkpoint
and numerical precision. Evaluation covers two masked DLMs, one prompt corpus, cohorts of
16--50 documents, and provenance only; payload recovery is not measured.

\clearpage
\bibliographystyle{plainnat}
\bibliography{main}

\clearpage
\appendix
\section{Additional Tables}
\label{app:tables}

Table~\ref{tab:validity} reports the measured per-key false-positive rate of the deployed
decision rule. Table~\ref{tab:robustness} summarises the post-editing evaluation under the
deployed pooled detector. Table~\ref{tab:quality_full} expands Table~\ref{tab:quality} into the
full task-accuracy grid, including the greedy-decoding cells.

\input{tables/validity_table}
\input{tables/robustness_table}
\input{tables/quality_table_full}

\section{Where the Watermark Can Be Written}
\label{app:viz}

Figure~\ref{fig:carriermap} replays the verifier's response bank over saved unwatermarked
512-token outputs and maps the admission score of Eq.~\eqref{eq:admission} across every
position. On this checkpoint the channel is wide: $97\%$ of positions retain probability
mass on both response signs and pass the $s_{\min}=0.5$ gate, and the sharp, unwritable
positions concentrate at the first tokens of a block, where the block-masked conditional is
most constrained. The two showcased conditionals make the criterion concrete: a good
carrier is a position where several plausible tokens disagree about the response sign, so
committing either sign stays on-distribution; a one-sided position has a single dominant
continuation, and the gate rejects it before the keyed orientation is revealed.

\input{figures/fig_carrier_viz}
\end{document}
