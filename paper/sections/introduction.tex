\section{Introduction}
\label{sec:intro}

\input{figures/fig_paradigm}

Language models now produce text that readers cannot reliably separate from human writing, which
turns provenance into a security problem, since disinformation, phishing, and academic fraud all
grow cheaper once authorship can no longer be established~\citep{liang2026survey}. Institutions
facing that problem reach first for a post-hoc detector, and academic-integrity screening is the
clearest case, given that an essay's authorship has to be settled after it is
submitted~\citep{krishna2023paraphrasing}. A detector of that kind is nevertheless fitted to one
generation process, and its verdicts weaken as deployment moves to models that decode by a
different mechanism, masked diffusion models among them. Watermarking
is the answer most widely deployed against it, since it plants a keyed statistical signal during
generation and lets whoever holds the key test a later document
for it~\citep{petitcolas1999hiding,kirchenbauer2023watermark}. A watermark is nevertheless worth
only the guarantee attached to its verdict, given that a false positive is an accusation against
a human author, and a deployment therefore needs a false-positive rate it can state in advance
rather than one fitted to a calibration corpus. Since
adversaries also paraphrase output and learn a key's behaviour from detector
queries~\citep{krishna2023paraphrasing,jovanovic2024stealing}, the guarantees worth designing
for are the ones that survive finite samples and adaptively chosen
trials~\citep{zhao2024provable,christ2024undetectable}.

Existing watermarks place their secret in the tokens, whether by biasing keyed vocabulary
subsets~\citep{kirchenbauer2023watermark}, by coupling each draw to
pseudorandomness~\citep{kuditipudi2024distortionfree}, or, in recent diffusion language model
(DLM) methods, by exploiting unresolved context and denoising order~\citep{hong2026dgmark}.
Detection then reduces to inspecting the text together with the key, and the generator is never
consulted. Masked DLMs, by contrast, permit a different arrangement,
since a finished text can be fed back to the model with selected context hidden and the
likelihood of an emitted token then moves by an amount the checkpoint alone determines. Hiding a
keyed subset of the context is in this sense a \emph{context probe}, the direction in which the
likelihood moves is the model's response to it, and provenance can be argued from the agreement
between the two. Such a probe is worth applying only where the model has a genuine choice of
answer, that is where several plausible tokens coexist at a masked position and react with
opposite signs to two patterns.

% --- Motivation figure (illustrative values, not measurements).  Commented out to
% --- hold the eight-page workshop limit now that Tables 2 and 3 carry the
% --- security-relevant evidence.  Uncomment to restore it at the cost of a page.
% \begin{figure}[t]
% \centering
% \includegraphics[width=\linewidth]{figures/fig_retrace_motivation.pdf}
% \caption{\textbf{The conditional ambiguity a probe requires.} Two replayable context views
% split a non-degenerate conditional's support by response sign into masses $q_+$ and $q_-$. The
% key requests one side, RAR selects from it, and RRV remeasures the emitted token's sign, with
% capacity limited by the weaker mass so that BCG rejects sharp or one-sided positions. The values
% shown are illustrative.}
% \label{fig:motivation}
% \end{figure}

Moving the secret out of the tokens and into that relation, however, costs two things a token-local detector obtains for nothing, since the verifier must apply exactly the probe the embedder
applied, and the choice of carriers must be settled before the accepted answer is known; a gate with sight of the rewarded sign would
otherwise inflate the false-positive rate above the level the detector reports.
Section~\ref{sec:setup} states both as requirements, and existing DLM watermarks avoid them by
keeping detection token-local, whether by removing prefix dependence~\citep{chen2025orderagnostic}, by averaging a
red--green bias over unresolved context~\citep{gloaguen2026watermarking}, or by steering which
mask is resolved next~\citep{hong2026dgmark}, none of which asks the model anything about the
text it produced.

Shibboleth instead meets each condition with one mechanism (Figure~\ref{fig:paradigm}). It settles
replayability by re-masking the current block together with its suffix before scoring the probe
family, and the state it measures therefore follows from the final text alone. A \emph{balanced gate} then admits as carriers only the unresolved positions whose block-masked
conditional retains mass on both response signs, scoring them in a way that is invariant to
swapping the two patterns and therefore blind to the sign the key will ask for.
Writing the answer is the easier half, since Shibboleth draws at most $R$ proposals from the
unchanged live row at an admitted position and commits the first one carrying that sign, which
amounts to rejection sampling and does change the output distribution, although every committed
token remains one the model proposed. Verification afterwards re-applies the same probe,
reapplies the gate, and returns an exact binomial tail. Unlike a prefix-based watermark, which can require strict left-to-right decoding, and unlike an
order-guided one, which changes which mask is resolved next, Shibboleth acts only after the
reference schedule has produced a row, and therefore leaves the block partition and the
confidence order untouched.

\begin{itemize}[leftmargin=1.2em,itemsep=1pt,topsep=2pt]
\item We show across 36 settings that this checkpoint's conditionals are too sharp for answers
to be inserted after the fact, and the answer must therefore be written while the conditional is
still live (Section~\ref{sec:posthoc}).
\item We propose Shibboleth, in which block-and-suffix re-masking makes the probe replayable from
the finished text and orientation-blind gating keeps the accepted answer independent of how the
carriers were discovered, which together keep the detector's $p$-values exact and free of
calibration (Sections~\ref{sec:observable} to~\ref{sec:theory}).
\item We evaluate Shibboleth on LLaDA-8B-Instruct and Dream-7B-Instruct and report both sides
of the trade, since the strongest setting reaches $0.80$ and $0.90$ TPR at $1\%$ FPR — on
LLaDA at the best
perplexity ratio of any method compared — whereas verification spends 144 masked evaluations
and $10\%$ re-denoising collapses TPR at $R=1$ and halves it at the strongest setting
(Section~\ref{sec:experiments}).
\end{itemize}
