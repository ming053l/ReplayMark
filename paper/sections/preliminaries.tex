\section{Shibboleth: Watermarking by Model Response}
\label{sec:method}

\subsection{Decoding and Notation}
\label{sec:prelim}

Masked diffusion language models differ from autoregressive models in that they predict a
missing token from an arbitrary subset of revealed tokens, and the same sequence can
consequently be produced under many decoding orders. Let $x$ be a prompt and $y=(y_1,\ldots,y_N)$ the continuation to be
generated, and for a revealed set $I\subset\{1,\ldots,N\}$ with a target position $i\notin I$,
the model supplies a predictor $p_\theta(y_i\mid y_I,x)$ while the remaining positions carry the
mask symbol, so that one sequence evaluation returns that distribution at every unresolved
position at once.

Deployed decoders often restrict that freedom; we study LLaDA's
semi-autoregressive schedule, in which the continuation is partitioned into blocks that finish from left to right
while diffusion resolves the masks inside the active
block~\citep{arriola2025blockdiffusion,nie2025llada}, and the reference confidence rule decides
which positions commit at each step. Everything below is therefore stated for one block $B_b$
with the prefix $F_b$ that precedes it.

\subsection{Problem Setup}
\label{sec:setup}

Our goal is a watermark whose detector consults the model rather than the text alone, telling a
continuation generated under a secret key from one generated without it at a false-positive rate
the verifier states in advance, while preserving the decoding schedule and measuring changes in
output quality.

\paragraph{Embedder and verifier.}
The embedder receives the model, the prompt, a secret key $K$, and the target bits, whereas the
verifier later receives only the completed sequence, the same model, $K$, and the public
parameters, without logits, random seeds, or commit order. Each document then draws its own key from
$K$ and a fresh nonce through a keyed hash, $K_d=\mathrm{HMAC}(K,\mathrm{nonce}_d)$, which to
anyone without $K$ behaves as a random function of the nonce~\citep{bellare1996hmac}. That
document key generates both the probe family and the accepted answers, and the false-positive
guarantee covers any text produced independently of it.

\paragraph{Threat model.}
Both the model and the algorithm may be public, since secrecy rests entirely in $K$. For every text written
independently of the document key, including machine text from another sampler, a detector run at
nominal level $\alpha$ fires with probability at most $\alpha$, and the bound holds at finite
sample size and under carriers the method itself selects. We claim nothing
in the complementary direction, and an adversary willing to edit the output can therefore suppress
detection, as Section~\ref{sec:calibration} measures. We likewise claim nothing after key disclosure,
under the detector-query access that recovers a key's behaviour~\citep{jovanovic2024stealing},
or when verification runs a numerically different checkpoint.

\paragraph{Post-hoc substitution.}
\label{sec:posthoc}
Post-hoc token substitution avoids intervention during sampling. We measure the number of
available replacements under this design. Write $\tau$ for the
likelihood budget of a substitution, the number of nats by which a replacement may fall below
the live argmax. Across 36 settings that replace tokens after ordinary generation, the median
selected position admits exactly one token at $\tau=3$, the mean rises from $1.6$ at
$\tau=3$ to $5.6$ at $\tau=6$, and the highest TPR inside a $1.35\times$ perplexity budget is
$0.10$ at $1\%$ FPR. The evaluated method therefore embeds while the conditional is available
during generation.

\medskip\noindent
We accordingly formalise the problem with three requirements.

\begin{enumerate}[leftmargin=1.6em,itemsep=1pt,topsep=2pt,label=\arabic*.]
\item \textbf{Replayability.} The verifier must be able to put exactly the question the embedder
put, from the finished text and the key alone, with no generation trajectory, stored logits, or
record of rejected samples.
\item \textbf{Blind selection.} The set of positions that carry a bit may depend arbitrarily on
the text and the model, but that choice has to be settled before the sign the key rewards is
consulted, since otherwise the reported false-positive rate understates the true one.
\item \textbf{Schedule preservation.} The block partition and the confidence order must survive
unchanged, so that a production decoder keeps the latency and quality profile it was tuned for.
\end{enumerate}

\noindent
The first two requirements interact: every scored context must be recoverable from the generated
text, while carrier selection must remain independent of the keyed sign.
Sections~\ref{sec:observable} to~\ref{sec:theory} specify the corresponding mechanisms.
