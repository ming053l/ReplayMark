% Downstream task quality (the "generated text quality" table). NOT yet \input anywhere —
% un-comment the \input in sections/experiments.tex once at least the LLaDA block is filled.
%
% The earlier GPT draft's version held a 2-model x 3-benchmark x 2-decoder grid whose numbers
% could not be traced to any run; it was deleted. Do not restore it from git history.
% Regenerate cells ONLY from:
%   LLaDA block  <- exp/32, results/32_gsm8k.json   (running in the chain)
%   Dream block  <- exp/39, results/39_dream_gsm8k.json (queued, run36.sh)
%
% Shared design: 50 GSM8K test problems, 256 generated tokens, matched unwatermarked control
% vs the watermarked R=16/kappa=0.05 arm under the identical decoding schedule; accuracy is
% exact match on the final number; detection (TPR@1%) is run on both arms — the control must
% sit at the null, and short docs mean the watermark arm's TPR here is not the headline.
% MMLU / HumanEval columns can be added later only if runs exist; do not pre-fill them.
\begin{table}[t]
\centering
\small
\renewcommand{\arraystretch}{0.8}
\begin{tabular}{llrr}
\toprule
Model & Arm & GSM8K Acc $\uparrow$ & TPR at $1\%$ FPR \\
\midrule
LLaDA-8B-Instruct & Unwatermarked control & \tbd & \tbd \\
\rowcolor{finalres}
      & \textcolor{oursemph}{Shibboleth} $R=16,\kappa=0.05$ & \tbd & \tbd \\
\midrule
Dream-7B-Instruct & Unwatermarked control & \tbd & \tbd \\
\rowcolor{finalres}
      & \textcolor{oursemph}{Shibboleth} $R=16,\kappa=0.05$ & \tbd & \tbd \\
\bottomrule
\end{tabular}
\caption{\textbf{Downstream task preservation.} GSM8K~\citep{cobbe2021gsm8k} accuracy on 50
test problems at 256 tokens under the watermark against a matched unwatermarked control on the
identical decoding schedule, with detection run on both arms. \tbd\ pending exp/32 (LLaDA) and
exp/39 (Dream).}
\label{tab:quality}
\end{table}
